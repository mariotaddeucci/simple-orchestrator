# simple-orchestrator-webapi

FastAPI application responsible for:

- All interaction with the SQLite database (queue, agents, sessions, memory)
- Providing a stable HTTP interface for `simple-orchestrator-worker` and `simple-orchestrator-tui`
